<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uspring.sdsmesplus.mpmlink.dao.ProcessPlanDao">

<select id="queryBom" resultType="String">
  select * from (
     select   FILE_ADDRESS
       from  PDM_MES_PROCESSPLAN
       where SIMPLE_NUMBER = #{prodCode}
           and PROCESSPLAN_NUMBER = #{lineCode} order by SEQ_ID )
    where rownum=1       
  </select>
</mapper>